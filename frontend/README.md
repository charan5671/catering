# Srinu Lunch Basket - Frontend 🎨

The premium, customer-facing web application for Srinu Lunch Basket.

## ✨ Features

- **Tailwind CSS 4**: Modern styles and transitions.
- **Glassmorphism**: Elegant transparent UI elements.
- **Booking Flow**: Multi-step booking simulation synced with WhatsApp.
- **Optimized Fonts**: Using Google Fonts (Outfit) for a premium look.

## 🚀 Scripts

- `npm run dev`: Start Next.js development server.
- `npm run build`: Create an optimized production build.
- `npm start`: Start the production server.

## 📞 WhatsApp Configuration

The main logic for WhatsApp message generation is located in `src/app/page.tsx`. Ensure the phone number in the `handleBookingSubmit` function matches the business number.
